# Needs

- [ ] Being able to generate a policy map (need states / actions)
  - Need to know if state is terminal (could be asked to environment)
  - State iterator
  - Action iterator -> action random sampler
- [ ] Being able to generate a new state value map (need states)
  - State iterator
- [ ] State + Action -> new state and reward (need environment context)
  - Transition function that knows about the environment
- [ ] Initialize state value map (need states)

  - State iterator
  - Action iterator

- need to be able to pick valid state (sarsa and q-learning)
